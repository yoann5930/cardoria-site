name: SEO sitemap dates CI

on:
  pull_request:
    paths:
      - backend/lib/seo/sitemap.js
      - backend/server.js
      - tests/seo-sitemap-lastmod.test.mjs
      - tests/seo-license-redirect.test.mjs
      - scripts/audit-public-seo.mjs
      - .github/workflows/seo-sitemap-lastmod-ci.yml

permissions:
  contents: read

jobs:
  seo-sitemap-dates:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - uses: actions/checkout@v4
        with:
          persist-credentials: false
      - uses: actions/setup-node@v4
        with:
          node-version-file: backend/.node-version
      - name: Check production modules and audit syntax
        run: |
          node --check backend/lib/seo/sitemap.js
          node --check backend/server.js
          node --check scripts/audit-public-seo.mjs
      - name: Install backend packages without lifecycle scripts
        run: npm --prefix backend install --ignore-scripts --omit=dev --no-audit --no-fund
      - name: Test sitemap dates and actual Express license routes
        run: node --experimental-vm-modules --test tests/seo-sitemap-lastmod.test.mjs tests/seo-license-redirect.test.mjs
  public-seo-diagnostic:
    runs-on: ubuntu-latest
    timeout-minutes: 4
    steps:
      - uses: actions/checkout@v4
        with:
          persist-credentials: false
      - uses: actions/setup-node@v4
        with:
          node-version-file: backend/.node-version
      - name: Read-only public SEO diagnostic (not an indexing guarantee)
        run: node scripts/audit-public-seo.mjs
