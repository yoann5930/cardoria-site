import { randomUUID, randomBytes, createHash } from "node:crypto";
import { getLiveSession } from "./sessions.js";
import { createCloudflareSession, publishCloudflareTracks, subscribeCloudflareTracks, renegotiateCloudflareSession, isCloudflareRealtimeConfigured } from "./cloudflare-realtime.js";

const publishedLives=new Map(),viewerSessions=new Map(),publisherPairs=new Map();
const VIEWER_TTL_MS=90_000,PAIR_TTL_MS=10*60_000,PUBLISHER_TTL_MS=120_000,MAX_PUBLISHERS_PER_LIVE=2;
const LIVE_PUBLISHER_ADMIN_ROLES=["super_admin","admin","employee"];
function requireLive(liveId){const live=getLiveSession(liveId);if(!live||live.status!=="live"){const e=new Error("Ce Live n'est pas disponible.");e.status=404;e.code="LIVE_SESSION_NOT_FOUND";throw e;}return live;}
function assertPublisher(live,actor){if(!actor){const e=new Error("Authentification diffuseur requise.");e.status=401;throw e;}const admin=LIVE_PUBLISHER_ADMIN_ROLES.includes(String(actor.role||"").toLowerCase()),sellerOwner=live.ownerRole==="seller"&&String(live.ownerId||"")===String(actor.id||actor.sellerId||""),adminOwner=live.ownerRole==="admin"&&admin;if(!sellerOwner&&!adminOwner){const e=new Error("Ce Live appartient à un autre diffuseur.");e.status=403;e.code="LIVE_PUBLISHER_MISMATCH";throw e;}}
function cleanSourceId(value){const raw=String(value||"primary").toLowerCase().replace(/[^a-z0-9_-]/g,"").slice(0,32);return raw||"primary";}
function livePublishers(liveId,create=false){let entry=publishedLives.get(liveId);if(!entry&&create){entry={sources:new Map(),startedAt:new Date().toISOString()};publishedLives.set(liveId,entry);}return entry;}
function allPublishedTracks(liveId){const entry=livePublishers(liveId);return entry?[...entry.sources.values()].flatMap((source)=>source.tracks||[]):[];}
function pairHash(token){return createHash("sha256").update(String(token||"")).digest("hex");}
function cleanupPairs(){const now=Date.now();for(const[hash,pair]of publisherPairs)if(pair.expiresAt<=now||pair.used)publisherPairs.delete(hash);}
function cleanupP2PSources(){const cutoff=Date.now()-PUBLISHER_TTL_MS;for(const[liveId,entry]of publishedLives){for(const[sourceId,source]of entry.sources){if(source.mode==="p2p"&&Number(source.lastSeenAt||0)<cutoff)entry.sources.delete(sourceId);}if(!entry.sources.size)publishedLives.delete(liveId);}}
export function cleanupRealtimeViewers(){const cutoff=Date.now()-VIEWER_TTL_MS;for(const[id,v]of viewerSessions)if(v.lastSeenAt<cutoff)viewerSessions.delete(id);cleanupPairs();cleanupP2PSources();}
const cleanupTimer=setInterval(cleanupRealtimeViewers,30_000);cleanupTimer.unref?.();
export function realtimeProvider(){return isCloudflareRealtimeConfigured()?"cloudflare-realtime":"cardoria-p2p";}
export function viewerCountForLive(liveId){cleanupRealtimeViewers();let count=0;for(const viewer of viewerSessions.values())if(String(viewer.liveId)===String(liveId))count++;return count;}
export function realtimeStatusForLive(liveId){cleanupRealtimeViewers();const entry=livePublishers(String(liveId||""));return{liveId:String(liveId||""),published:Boolean(entry?.sources.size),publishers:entry?.sources.size||0,viewers:viewerCountForLive(liveId),maxPublishersPerLive:MAX_PUBLISHERS_PER_LIVE,sources:entry?[...entry.sources.keys()]:[],provider:realtimeProvider()};}
export function createPublisherPair({liveId,actor,sourceId="secondary"}){const live=requireLive(liveId);assertPublisher(live,actor);cleanupPairs();const normalizedSource=cleanSourceId(sourceId),token=randomBytes(32).toString("base64url"),expiresAt=Date.now()+PAIR_TTL_MS;publisherPairs.set(pairHash(token),{liveId,sourceId:normalizedSource,actor:{role:actor.role,id:actor.id||actor.sellerId||"admin",sellerId:actor.sellerId||"",email:actor.email||""},expiresAt,used:false});return{token,liveId,sourceId:normalizedSource,expiresAt:new Date(expiresAt).toISOString()};}
export function claimPublisherPair(token){cleanupPairs();const hash=pairHash(token),pair=publisherPairs.get(hash);if(!pair||pair.used||pair.expiresAt<=Date.now()){const e=new Error("Lien caméra expiré ou invalide.");e.status=401;e.code="LIVE_CAMERA_PAIR_INVALID";throw e;}pair.used=true;publisherPairs.delete(hash);return pair;}
export async function startRealtimePublisher({liveId,actor,offer,tracks,sourceId="primary"}){const live=requireLive(liveId);assertPublisher(live,actor);const normalizedSource=cleanSourceId(sourceId),entry=livePublishers(liveId,true);if(!entry.sources.has(normalizedSource)&&entry.sources.size>=MAX_PUBLISHERS_PER_LIVE){const e=new Error("Deux caméras maximum sont autorisées sur ce Live.");e.status=409;e.code="LIVE_CAMERA_LIMIT";throw e;}
  if(isCloudflareRealtimeConfigured()){
    const cloudflareSessionId=await createCloudflareSession(),namedTracks=tracks.map((track)=>({...track,trackName:`${normalizedSource}-${track.kind==="video"?"camera":"microphone"}`})),published=await publishCloudflareTracks({cloudflareSessionId,offer,tracks:namedTracks});
    entry.sources.set(normalizedSource,{mode:"cloudflare",sourceId:normalizedSource,publisherId:String(actor.id||actor.sellerId||"admin"),cloudflareSessionId,tracks:published.tracks,startedAt:new Date().toISOString(),lastSeenAt:Date.now()});
    return{provider:"cloudflare-realtime",mode:"cloudflare",liveId,sourceId:normalizedSource,sourceCount:entry.sources.size,answer:published.answer};
  }
  const publisherKey=randomBytes(32).toString("base64url");
  entry.sources.set(normalizedSource,{mode:"p2p",sourceId:normalizedSource,publisherId:String(actor.id||actor.sellerId||"admin"),publisherKey,tracks:(tracks||[]).map((track)=>({kind:track.kind,trackName:track.trackName||`${normalizedSource}-${track.kind}`})),offers:new Map(),startedAt:new Date().toISOString(),lastSeenAt:Date.now()});
  return{provider:"cardoria-p2p",mode:"p2p",liveId,sourceId:normalizedSource,sourceCount:entry.sources.size,publisherKey};
}
function requireP2PSource({liveId,sourceId,publisherKey}){const entry=livePublishers(String(liveId||"")),source=entry?.sources.get(cleanSourceId(sourceId));if(!source||source.mode!=="p2p"){const e=new Error("Source caméra P2P introuvable.");e.status=404;e.code="LIVE_P2P_SOURCE_NOT_FOUND";throw e;}if(String(source.publisherKey)!==String(publisherKey||"")){const e=new Error("Clé diffuseur invalide.");e.status=401;e.code="LIVE_P2P_PUBLISHER_KEY_INVALID";throw e;}source.lastSeenAt=Date.now();return source;}
export function pollRealtimePublisherOffers({liveId,sourceId,publisherKey}){const source=requireP2PSource({liveId,sourceId,publisherKey});const offers=[];for(const[viewerId,item]of source.offers){if(!item.answer)offers.push({viewerId,offer:item.offer});}return{ok:true,liveId,sourceId:cleanSourceId(sourceId),offers};}
export function submitRealtimePublisherAnswer({liveId,sourceId,publisherKey,viewerId,answer}){const source=requireP2PSource({liveId,sourceId,publisherKey}),item=source.offers.get(String(viewerId||"")),viewer=viewerSessions.get(String(viewerId||""));if(!item||!viewer||viewer.liveId!==liveId){const e=new Error("Spectateur P2P introuvable.");e.status=404;e.code="LIVE_P2P_VIEWER_NOT_FOUND";throw e;}item.answer=answer;item.answeredAt=Date.now();viewer.answers.set(cleanSourceId(sourceId),answer);viewer.lastSeenAt=Date.now();return{ok:true,viewerId,sourceId:cleanSourceId(sourceId)};}
export function submitRealtimeViewerOffer({viewerId,sourceId,offer}){const viewer=viewerSessions.get(String(viewerId||""));if(!viewer||viewer.mode!=="p2p"){const e=new Error("Session spectateur P2P introuvable.");e.status=404;e.code="LIVE_P2P_VIEWER_NOT_FOUND";throw e;}const entry=livePublishers(viewer.liveId),normalizedSource=cleanSourceId(sourceId),source=entry?.sources.get(normalizedSource);if(!source||source.mode!=="p2p"){const e=new Error("Caméra P2P indisponible.");e.status=404;e.code="LIVE_P2P_SOURCE_NOT_FOUND";throw e;}source.offers.set(String(viewerId),{offer,createdAt:Date.now(),answer:null});viewer.lastSeenAt=Date.now();return{ok:true,viewerId,sourceId:normalizedSource};}
export function pollRealtimeViewerAnswers(viewerId){const viewer=viewerSessions.get(String(viewerId||""));if(!viewer||viewer.mode!=="p2p"){const e=new Error("Session spectateur P2P introuvable.");e.status=404;e.code="LIVE_P2P_VIEWER_NOT_FOUND";throw e;}viewer.lastSeenAt=Date.now();return{ok:true,viewerId:String(viewerId),liveId:viewer.liveId,answers:Object.fromEntries(viewer.answers)};}
export function stopRealtimePublisher({liveId,actor,sourceId}){const live=getLiveSession(liveId);if(live)assertPublisher(live,actor);const entry=livePublishers(liveId);if(entry){if(sourceId)entry.sources.delete(cleanSourceId(sourceId));else entry.sources.clear();if(!entry.sources.size){publishedLives.delete(liveId);for(const[id,v]of viewerSessions)if(v.liveId===liveId)viewerSessions.delete(id);}}return{ok:true,liveId,sourceId:sourceId?cleanSourceId(sourceId):null,sourceCount:entry?.sources.size||0};}
export async function startRealtimeViewer(liveId){cleanupRealtimeViewers();requireLive(liveId);const entry=livePublishers(liveId);if(!entry?.sources.size){const e=new Error("La diffusion vidéo n'est pas encore disponible.");e.status=404;e.code="LIVE_STREAM_NOT_PUBLISHED";throw e;}
  const useCloudflare=isCloudflareRealtimeConfigured()&&[...entry.sources.values()].every((source)=>source.mode==="cloudflare");
  if(useCloudflare){const tracks=allPublishedTracks(liveId),subscribed=await subscribeCloudflareTracks({tracks}),viewerId=randomUUID();viewerSessions.set(viewerId,{mode:"cloudflare",liveId,cloudflareSessionId:subscribed.cloudflareSessionId,lastSeenAt:Date.now()});return{provider:"cloudflare-realtime",mode:"cloudflare",viewerId,liveId,offer:subscribed.offer,mids:subscribed.mids,sourceCount:entry.sources.size,viewerCount:viewerCountForLive(liveId)};}
  const viewerId=randomUUID(),sources=[...entry.sources.values()].filter((source)=>source.mode==="p2p").map((source)=>({sourceId:source.sourceId,tracks:source.tracks||[]}));
  if(!sources.length){const e=new Error("La diffusion vidéo P2P n'est pas encore disponible.");e.status=404;e.code="LIVE_STREAM_NOT_PUBLISHED";throw e;}
  viewerSessions.set(viewerId,{mode:"p2p",liveId,lastSeenAt:Date.now(),answers:new Map()});
  return{provider:"cardoria-p2p",mode:"p2p",viewerId,liveId,sources,sourceCount:sources.length,viewerCount:viewerCountForLive(liveId)};
}
export async function answerRealtimeViewer({viewerId,answer}){const viewer=viewerSessions.get(viewerId);if(!viewer){const e=new Error("Session spectateur expirée.");e.status=404;e.code="LIVE_VIEWER_SESSION_NOT_FOUND";throw e;}viewer.lastSeenAt=Date.now();if(viewer.mode!=="cloudflare"){const e=new Error("Réponse spectateur non utilisée en mode P2P.");e.status=409;e.code="LIVE_P2P_ANSWER_NOT_EXPECTED";throw e;}await renegotiateCloudflareSession({cloudflareSessionId:viewer.cloudflareSessionId,answer});return{ok:true,liveId:viewer.liveId,viewerCount:viewerCountForLive(viewer.liveId)};}
export function heartbeatRealtimeViewer(viewerId){const viewer=viewerSessions.get(viewerId);if(!viewer)return{active:false,viewerCount:0};viewer.lastSeenAt=Date.now();const live=getLiveSession(viewer.liveId);return{active:Boolean(live&&live.status==="live"&&publishedLives.has(viewer.liveId)),liveId:viewer.liveId,viewerCount:viewerCountForLive(viewer.liveId),provider:viewer.mode==="p2p"?"cardoria-p2p":"cloudflare-realtime"};}
export function stopRealtimeViewer(viewerId){const id=String(viewerId||""),liveId=viewerSessions.get(id)?.liveId;viewerSessions.delete(id);if(liveId){const entry=livePublishers(liveId);for(const source of entry?.sources.values()||[])source.offers?.delete(id);}return{ok:true,liveId:liveId||"",viewerCount:liveId?viewerCountForLive(liveId):0};}
export function isRealtimePublished(liveId){return(livePublishers(String(liveId||""))?.sources.size||0)>0;}
export function isLivePublisher(live,actor){try{assertPublisher(live,actor);return true;}catch{return false;}}
export function realtimeStatus(){cleanupRealtimeViewers();let publishers=0;for(const entry of publishedLives.values())publishers+=entry.sources.size;return{published:publishedLives.size,publishers,viewers:viewerSessions.size,maxPublishersPerLive:MAX_PUBLISHERS_PER_LIVE,provider:realtimeProvider()};}
