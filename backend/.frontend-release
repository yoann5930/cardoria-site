cardoria-frontend-release=2026-08-28-admin-security-100
Includes: mandatory TOTP 2FA before every Admin/employee session; first-login Authenticator enrollment without creating a session; password and magic-link Admin login both gated by short-lived 2FA challenges; RFC-compatible Base32 TOTP secrets; failed-code attempt limits; Admin sessions without enabled 2FA rejected; browser verifies /api/auth/me and requires totpEnabled; legacy x-cardoria-admin-code fallback removed server-side and browser-side; legacy /api/admin/login authentication route deleted; dedicated Admin Security CI E2E proves no session exists before TOTP, valid enrollment/login, invalid-code rejection, and legacy header/route cannot authenticate. Production CI and security regression guards protect the flow.
