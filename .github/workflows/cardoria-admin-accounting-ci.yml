name: Cardoria Admin Accounting CI

on:
  pull_request:
    branches: [main]
    paths:
      - "backend/lib/accounting/**"
      - "backend/routes/admin.js"
      - "backend/tests/accounting-purchase-stats.test.mjs"
      - ".github/workflows/cardoria-admin-accounting-ci.yml"
  push:
    branches: [main]
    paths:
      - "backend/lib/accounting/**"
      - "backend/routes/admin.js"
      - "backend/tests/accounting-purchase-stats.test.mjs"
      - ".github/workflows/cardoria-admin-accounting-ci.yml"

jobs:
  accounting-audit:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-node@v7
        with:
          node-version: "22.22.0"
          package-manager-cache: false
      - name: Syntax checks
        run: |
          node --check backend/lib/accounting/purchase-stats.js
          node --check backend/routes/admin.js
      - name: Paid purchase accounting regression
        run: node --test backend/tests/accounting-purchase-stats.test.mjs
